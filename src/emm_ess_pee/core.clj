(ns emm-ess-pee.core
  (:use emm-ess-pee.binary-utils
        clojure.repl)
  (:require [clojure.pprint :refer [pprint]])
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
